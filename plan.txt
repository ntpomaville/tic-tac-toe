object to create game board
    3x3 grid of buttons
        buttons accept x or o, or nothing if filled

object to control game flow
    alternate changing buttons to x/o
    check button text for 3 in row (3 columns, 3 rows, 2 diagonal)
        if yes, declare winner, reset

object to reset game
    reset text on all buttons

button to reset game (call object)
